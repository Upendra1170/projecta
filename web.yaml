- name: install web components
  host: web
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.dnf:
      name: nginx
      state: present

- name: remove html directory
  ansible.builtin.file:
    path: /usr/share/nginx/html
    state: absent

- name: create html directory
  ansible.builtin.file:
    path: /usr/share/nginx/html
    state: directory

- name: download web app
  ansible.builtin.get_url:
    url: https://roboshop.s3.amazonaws.com/web.zip
    dest: /tmp/web.zip  

- name: extract web app
  ansible.builtin.unarchieve:
    src: /temp/web.zip
    dest: user/share/nginx/html
    remote_src: yes

    